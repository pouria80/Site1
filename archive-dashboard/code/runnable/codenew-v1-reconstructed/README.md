# PooriTel codenew v1 — runnable reconstruction

The page component in `src/pages/CustomerHub.tsx` is the source from `code/codenew.txt`. The TXT references external components/files that were not present in the TXT itself, so this runnable folder provides a compatibility layer for those imports.

Run:

```bash
npm install
npm run dev
```

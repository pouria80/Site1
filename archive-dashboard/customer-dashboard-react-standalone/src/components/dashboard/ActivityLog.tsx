import React from 'react';import '../../css/dashboard-activity.css';export default function ActivityLog({activities}:{activities:any[]}){return <section className="card"><div className="row"><h3>Important Activity</h3><small>{activities.length} events</small></div>{activities.map(a=><div className="activity" key={a.id}><span>•</span><div><b>{a.description}</b><small>{new Date(a.timestamp).toLocaleString()}</small></div></div>)}</section>}

import React from 'react';import '../../css/dashboard-orders.css';export default function RecentOrders({orders}:{orders:any[]}){return <section className="card"><div className="row"><h3>Recent Orders</h3><a>View all →</a></div>{orders.map(o=><div className="order" key={o.id}><div><b>{o.product}</b><small>#{o.id}</small></div><div><b>${Number(o.amount).toFixed(2)}</b><span>{o.status}</span></div></div>)}</section>}

# PooriTel Customer Dashboard Standalone

A separate React/Vite copy of the Customer Dashboard for design review and local testing.

## Run

```bash
npm install
npm run dev
```

The preview tries `/api/customer/dashboard` first and falls back to mock data when the standalone app is not connected to the Worker API.

This folder is isolated from the production login/auth application.

import React from 'react';import '../../css/dashboard-balance.css';export default function BalanceHero({wallets,selectedCurrency,onCurrencyChange}:{wallets:any[];selectedCurrency:'USD'|'IRT';onCurrencyChange:(c:'USD'|'IRT')=>void}){const w=wallets.find(x=>x.currency===selectedCurrency);return <section className="card balance-hero"><div className="row"><h2>Available Balance</h2><div><button onClick={()=>onCurrencyChange('USD')} className={selectedCurrency==='USD'?'active':''}>USD</button><button onClick={()=>onCurrencyChange('IRT')} className={selectedCurrency==='IRT'?'active':''}>IRT</button></div></div><div className="balance">{selectedCurrency==='USD'?'$':'﷼'} {Number(w?.balance||0).toLocaleString()}</div><div className="balance-actions"><button>Charge Wallet</button><button>Withdraw</button></div></section>}

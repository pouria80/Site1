let dictionaries={};let currentLanguage=localStorage.getItem('pooritel-language')||'fa';
async function loadLocale(language){const response=await fetch(`/locales/${language}.json`,{cache:'no-store'});if(!response.ok)throw new Error(`Failed to load ${language}.json`);return response.json()}
function getValue(object,path){return path.split('.').reduce((acc,key)=>acc?.[key],object)}
function t(key){return getValue(dictionaries[currentLanguage],key)??key}
async function syncAccount(){try{const r=await fetch('/api/auth/me',{credentials:'include',cache:'no-store'});const d=r.ok?await r.json():null;const header=document.querySelector('.product-header-actions');if(!header)return;let account=document.getElementById('productAccount');if(d?.authenticated){const name=d.user?.email?.split('@')[0]||d.user?.email||'Account';if(!account){account=document.createElement('a');account.id='productAccount';account.className='back-link';header.appendChild(account)}account.textContent=name;account.href='/dashboard/';account.title=d.user?.email||name}else if(account){account.remove()}}catch{}}
function applyLanguage(language){if(!dictionaries[language])return;currentLanguage=language;localStorage.setItem('pooritel-language',language);const isFa=language==='fa';document.documentElement.lang=language;document.documentElement.dir=isFa?'rtl':'ltr';document.querySelectorAll('[data-i18n]').forEach(el=>{el.textContent=t(el.dataset.i18n)});document.title=t('meta.productTitle');document.getElementById('currentFlag').textContent=isFa?'🇮🇷':'🇺🇸';document.getElementById('currentLanguageName').textContent=isFa?'فارسی':'English';document.getElementById('targetFlag').textContent=isFa?'🇺🇸':'🇮🇷';document.getElementById('targetLanguageName').textContent=isFa?'English':'فارسی';updatePrices();syncAccount()}
function updatePrices(){const isFa=currentLanguage==='fa';document.querySelectorAll('.price-value').forEach(el=>{const fa=Number(el.dataset.priceFa),en=Number(el.dataset.priceEn);el.textContent=isFa?new Intl.NumberFormat('fa-IR').format(fa):`$${en.toFixed(2)}`});const badge=document.getElementById('currencyBadge');if(badge)badge.textContent=isFa?'تومان':'USD'}
function closeMenu(){const menu=document.getElementById('languageMenu'),trigger=document.getElementById('langTrigger');menu.classList.remove('open');trigger.setAttribute('aria-expanded','false')}
async function init(){dictionaries.fa=await loadLocale('fa');dictionaries.en=await loadLocale('en');if(!dictionaries[currentLanguage])currentLanguage='fa';applyLanguage(currentLanguage);const trigger=document.getElementById('langTrigger'),menu=document.getElementById('languageMenu'),option=document.getElementById('languageOption');trigger.addEventListener('click',()=>{const open=!menu.classList.contains('open');menu.classList.toggle('open',open);trigger.setAttribute('aria-expanded',String(open))});option.addEventListener('click',()=>{applyLanguage(currentLanguage==='fa'?'en':'fa');closeMenu()});document.addEventListener('click',event=>{if(!event.target.closest('.language-wrap'))closeMenu()});document.getElementById('buyButton').addEventListener('click',()=>alert(t('product.demoBuyMessage')))}
init().catch(error=>console.error('Product page init failed:',error));
